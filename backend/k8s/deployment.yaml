apiVersion: apps/v1
kind: Deployment

metadata:
  name: hotstar-backend

spec:
  replicas: 2

  selector:
    matchLabels:
      app: hotstar-backend

  template:
    metadata:
      labels:
        app: hotstar-backend

    spec:
      containers:
        - name: hotstar-backend

          image: sevenajay/hotstar-backend:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 8080

          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
